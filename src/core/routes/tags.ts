import { Router } from 'express'

const router: Router = Router()

router.get('/', () => {})

router.post('/', () => {})

router.put('/:tagId', () => {})

router.delete('/:tagId', () => {})

export { router as tagRoutes }
